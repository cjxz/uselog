<?xml version="1.0" encoding="UTF-8"?>

<included>
    <springProperty scope="context" name="enable" source="fastlog.enable" defaultValue="true"/>
    <springProperty scope="context" name="url" source="fastlog.url" defaultValue="localhost:9092"/>
    <springProperty scope="context" name="topic" source="fastlog.topic" defaultValue="log"/>
    <springProperty scope="context" name="type" source="fastlog.type" defaultValue="kafka"/>
    <springProperty scope="context" name="batchMessageSize" source="fastlog.batchMessageSize" defaultValue="128"/>
    <springProperty scope="context" name="partition" source="fastlog.partition" defaultValue="4"/>
    <springProperty scope="context" name="additionalFields" source="fastlog.additionalFields"/>

    <appender name="FAST_LOG" class="com.zmh.fastlog.FastLogAppender">
        <config>
            <enable>${enable}</enable>
            <url>${url}</url>
            <topic>${topic}</topic>
            <batchMessageSize>${batchMessageSize}</batchMessageSize>
            <type>${type}</type>
            <partition>${partition}</partition>
            <!--<additionalFields>${additionalFields}</additionalFields>-->
        </config>
    </appender>

</included>
